{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Python: Current File (cwd = file dir)",
      "type": "python",
      "request": "launch",
      "program": "${file}",
      "console": "integratedTerminal",
      "cwd": "${fileDirname}"
    }
  ]
}
